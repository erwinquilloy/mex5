# LIBERO sim deps -- install only when running sim benchmarks.
# Install LIBERO from source per its README:
#   git clone https://github.com/Lifelong-Robot-Learning/LIBERO.git
#   cd LIBERO && pip install -r requirements.txt && pip install -e .
mujoco>=3.1
robosuite>=1.4
numpy
pillow
hydra-core
easydict
